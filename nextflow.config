profiles {
  nimbus {
      workDir = '/data/human/spleen/nxf_spleen'
      
      process.container = 'nextflow/nextflow-spleen:latest'
      
      docker {
        docker.enabled = true
        docker.runOptions = '-u $(id -u):$(id -g)'
    }
}
}